import { Flex } from "@chakra-ui/react";
type Props = {};

const Dashboard = (props: Props) => {
  return <Flex className="h-full w-full bg-yellow-300">Hello There</Flex>;
};

export default Dashboard;
